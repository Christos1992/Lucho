<%= simple_form_for([ current_user.parent, incident, side_effect_incident ], remote: true) do |form| %>
  <%= form.input "side_effect_id", as: :select, collection: @allsideeffects, placeholder: 'What were the side effects?' %>
  <%= form.button :submit %>
<% end %>
