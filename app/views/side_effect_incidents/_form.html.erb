<%= simple_form_for([ current_user.parent, incident, side_effect_incident ], remote: true) do |form| %>
  <%= form.input "side_effect_id", as: :select, collection: @allsideeffects.uniq, placeholder: 'What were the side effects?' %>

  <%  @side_effect_names.each do  | side_effect_name|  %>

   <%= form.input "side_effect_id", as: :select, collection: SideEffect.where(name: side_effect_name).map{ |se|  [se.metric, se.id]} , placeholder: 'What were the side effects?', wrapper_html: { class: 'hide-this', id: "metric-#{side_effect_name}" }%>
  <% end %>
  <%= form.button :submit %>
<% end %>
<script type="text/javascript">

 select input
  add event listener  ---> change

  event.currentTarget.value ===> name of sideeffect

  get document input through  its id == 'metric' + name of side effect
  this document. classlist. remove ( hide this class)


</script>
